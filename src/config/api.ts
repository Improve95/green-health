// Toggle between real backend and stubs
// Set to true to use the real backend, false for local stubs
export const USE_REAL_BACKEND = false;

// Base URL for the backend API
export const API_BASE_URL = 'http://localhost:8080/api/v1';

// WebSocket base URL
export const WS_BASE_URL = 'ws://localhost:8080/ws';

// Polling interval for report status updates (ms)
export const REPORT_POLL_INTERVAL = 5000;
